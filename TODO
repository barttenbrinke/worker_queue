The following things need to be done:

- Named finders (causes problems with un-initialized WorkerQueueclass, needs solution)
- Max parallalization: WQ now does either parallel or sequential execution. It needs a 3rd variant: max sequential per class_name.